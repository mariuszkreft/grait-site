# GRAIT Site

Personal positioning website for GRAIT: coaching and forward deployed engineering.

## Run

```bash
npm start
```

The app listens on `PORT` or `8080`.

## Check

```bash
npm run check
```

## Deploy

This is a static Node server with a Dockerfile for Coolify. It exposes `/healthz` for health checks.
