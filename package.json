{
  "name": "grait-site",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node scripts/check-static.js"
  },
  "engines": {
    "node": ">=20"
  }
}
